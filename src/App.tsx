import { useState } from "react";

import "./index.css";
function App() {
  const [count, setCount] = useState(0);

  return <h1 className="text-blue-900">Employer menager</h1>;
}

export default App;
